// Vercel serverless entrypoint reusing the Express app
const app = require('../backend/app');

module.exports = app;
// Vercel serverless entrypoint for the Express app
const app = require('../backend/app');

module.exports = app;

// Vercel serverless entrypoint for the Express app
const app = require('../backend/app');

module.exports = app;